---
import Layout from '@layouts/Default.astro';
import RecentBlogPosts from '@components/generic/RecentBlogPosts.astro';
import MobileSocials from '@components/home/MobileSocials.astro';
import { Pill } from '@eliancodes/brutal-ui';
import { Button } from '@eliancodes/brutal-ui';
import { Card } from '@eliancodes/brutal-ui';

const Speakers = [
  { name: 'Piyush Wairale', role: 'Teaching Fellow', image: 'https://wayanadiitm.github.io/ml/events/logicloom2/storage/piyushsir2.PNG', bio: '', topic: '' },
  { name: 'Jyothi Krishnan', role: 'Assnt Prof', image: 'https://wayanadiitm.github.io/ml/events/logicloom2/storage/jk.png', bio: '', topic: '' },
];
---

<Layout
  title='Program Committee'
  pageTitle='LogicLooM 4.0 | Workshops'
  description='Welcome to LogicLooM, the most celebrated technical event at Paradox, the annual flagship techno-cultural fest for IIT Madras BS students'
>
<main class="bg-pink p-6">
  <section id="about" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-3 gap-8 mt-4">
<h2 class="text-2xl md:text-4xl lg:text-6xl mb-4 dm-serif">
        Distinguished Speakers
      </h2>
{Speakers.map((speaker) => (
  <div className="relative flex justify-center items-center" key={speaker.name}>
    <Card className="overflow-hidden shadow-xl rounded-[30px] w-full h-full p-6 hover:scale-105 transition-transform duration-300 ease-in-out">
      <div className="w-32 h-32 relative mx-auto -mt-16 mb-4">
        <img
          src={speaker.image}
          alt={speaker.name}
          className="w-full h-full object-cover absolute top-0 left-0 rounded-[30px] clip-path-[polygon(50%_0%,100%_20%,100%_80%,50%_100%,0%_80%,0%_20%)] shadow-xl grayscale"
        />
      </div>
      <h3 className="text-2xl text-center font-bold text-gray-800">{speaker.name}</h3>
      <p className="text-center text-gray-500 mt-2 text-sm">{speaker.role}</p>
      <p className="text-center text-gray-400 mt-2 text-sm">{speaker.bio}</p>
      <button className="mt-4 block mx-auto text-pink-600 hover:text-pink-800 transition-all duration-300 text-sm font-semibold">
        View More
      </button>
    </Card>
  </div>
))}
  </section>
</main>
</Layout>
